Patch the existing Android app only.

Goals:
1. Rename Library to My Library everywhere in the UI.
2. Add a detail screen when selecting a title from Books and My Library.
3. Detail screen must show:
   - cover
   - title
   - Stephen King / as Richard Bachman flag where relevant
   - year / release date
   - decade
   - story type
   - genre(s)
   - keywords
   - word count
   - audible length
   - adaptation flag / IMDb link
   - notes / comments if available
   - collection membership info
4. Detail screen must also expose the same actions available via long press.
5. For collections, detail view must include a tidy list/catalog of child stories in that collection.
6. Books and My Library must both support:
   - Sort
   - Filter
   - decade filter
   - Bachman filter
   - separate Genre filter
   - separate Keyword filter
7. Preserve build stability and offline-first behavior.
